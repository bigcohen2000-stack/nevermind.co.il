import { NotFoundView } from "@/components/layout/not-found-view";

/**
 * Site-segment 404 for notFound() inside (site) routes.
 * SiteShell comes from (site)/layout.
 */
export default function SiteNotFound() {
  return <NotFoundView />;
}
